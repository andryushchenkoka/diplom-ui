# url
urlBase=https://demowebshop.tricentis.com
urlRemote=https://${remoteLog}:${remotePass}@selenoid.autotests.cloud/wd/hub

# browser
browserName=${browser}
browserVersion=${browserVersion}
browserSize=${browserSize}

# timeout
timeout=20000